# NPM Configuration for Base Ninja Game

# Use legacy peer dependency resolution to avoid conflicts
legacy-peer-deps=true

# Reduce npm warnings during build
loglevel=error

# Use exact versions for reproducible builds
save-exact=true

# Skip optional dependencies that fail (like hardhat in production)
optional=true
